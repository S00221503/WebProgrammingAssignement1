const mongoose = require('mongoose');

const contactSchema = new mongoose.Schema({
     location: String, // Change the field name to lowercase
     weather: String
})

const Weather = mongoose.model('weather', contactSchema);

module.exports = { Weather };
